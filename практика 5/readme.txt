libs:

telebot
func main.py:

    start - processes the command /start
    help - processes the command /help
    who - processes the command /who
    хочу - sends user link to MTUSI frontpage
    не хочу - does not sends user link to MTUSI frontpage